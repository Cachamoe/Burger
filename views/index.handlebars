<h1>Burgers!</h1>

<h2>Burgers that !</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    <li> {{burger_name}} <button class="devouredBtn"> Devoured </button></li>
    {{/unless}}
    {{/each}}
</ul>

<h2></h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
   <li> {{burger_name}} </li>
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="burgerName" name="name">
    </div>


    <button class="addBurgerBtn" type="button">Add Burger</button>